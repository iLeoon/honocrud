# shadcn-login-app

Minimal Next.js app with a shadcn-styled login form (component at components/login-form.tsx).

To run:

1. npm install
2. npm run dev

You may need to install additional shadcn-specific packages if you want exact feature parity with the shadcn blocks site.
